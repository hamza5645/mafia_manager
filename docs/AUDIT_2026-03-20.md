# Mafia Manager Audit

Date: 2026-03-20

Scope: solo flow, multiplayer flow, Supabase RPC/schema, and test/build configuration.

Verification performed:
- Xcode workspace build succeeded after the remediation pass.
- `xcodebuild -project mafia_manager.xcodeproj -scheme mafia_manager -destination "platform=iOS Simulator,name=iPhone 17 Pro" test` now succeeds.
- Automated verification now covers 17 `GameStoreTests` cases, including the solo tie/day-advance regression and the 2-vs-2 Mafia win condition.
- Live Supabase inspection is still available only in read-only mode in this session:
  - I verified the deployed function bodies for `add_session_player`, `get_visible_role`, round-aware `submit_game_action`, `resolve_night_atomic`, and `batch_assign_roles`;
  - the repo now contains the corrective migrations for those functions;
  - I could not apply those migrations to the deployed project from this session because the MCP bridge rejected DDL in read-only mode.

## Summary

- Critical: 2
- High: 7
- Medium: 4
- Low: 3
- Repo status: MM-01 through MM-16 are fixed in code/schema/docs as of 2026-03-21.
- Verified deployed on Supabase: MM-01.
- Still pending live Supabase deployment because the current DB bridge is read-only: MM-04, MM-05, MM-12, MM-13, MM-14.

## Confirmed Defects

### MM-01 [Critical] `resolve_night_atomic()` is reading the wrong JSON keys and can wipe prior night history

Status: Fixed on 2026-03-21

Verification update:
- Deployed `resolve_night_atomic()` now reads `night_index` first and tolerates legacy `nightIndex` only as a fallback.
- Deployed `resolve_night_atomic()` now rejects malformed payloads with SQLSTATE `22023` instead of silently nulling `v_night_index`.
- Live verification still shows `night_history` payloads use snake_case keys; the backend drift described below has been corrected going forward.
- This is a fix-forward change only; previously collapsed `night_history` arrays were not backfilled by MM-01.

Confidence: High

Files:
- `Core/Models/Multiplayer/GameSession.swift`
- `Core/Services/Multiplayer/SessionService.swift`
- `supabase/multiplayer_schema.sql`

Evidence:
- `NightActionRecord` encodes snake_case keys such as `night_index` and `is_resolved`.
- `SessionService.resolveNightAtomic()` sends that encoded JSON record to Supabase.
- `resolve_night_atomic()` reads `p_night_record->>'nightIndex'` and filters existing history with `elem->>'nightIndex'`.
- Live database check:
  - 93 sessions contain `night_index` keys in `night_history`;
  - 0 sessions contain `nightIndex`;
  - all 16 sessions with `day_index >= 2` have `night_history` length `<= 1`.

Impact:
- `v_night_index` becomes `NULL`.
- The SQL filter does not match existing entries correctly.
- `night_history` can collapse to just the latest record on each atomic resolution.
- `removal_note = 'Eliminated at night ' || v_night_index` also becomes null-ish behavior because `v_night_index` is null.

Key references:
- `Core/Models/Multiplayer/GameSession.swift` encodes `night_index`
- `Core/Services/Multiplayer/SessionService.swift:356-454`
- `supabase/multiplayer_schema.sql:484-539`

Why players would feel this:
- later rounds lose accurate history;
- bot targeting and summaries can drift because prior nights silently disappear.

### MM-02 [Critical] Multiplayer doctor saves can be lost when doctors split their protections

Status: Fixed in app code path on 2026-03-21; build-verified

Verification update:
- `NightActionRecord` now persists `target_was_saved`, so phase 1 stores the authoritative save verdict instead of relying on a summarized protection target.
- `recordNightActions()` now records that verdict and biases `doctorProtectedId` toward the mafia target when any doctor successfully saved them.
- `resolveNightOutcome()` now resolves from the stored verdict, with a compatibility fallback for unresolved legacy records that still lack `target_was_saved`.
- `MultiplayerNightView` no longer recomputes save state from `mafiaTargetId == doctorProtectedId`; the store remains the source of truth.
- Multiplayer summaries now read `target_was_saved` when available so saved nights are reported consistently.
- App build verification succeeded after the fix.
- Live room verification reproduced the split-doctor setup on 2026-03-21; final end-to-end morning-screen confirmation was partially blocked by a separate host night-advance/readiness issue.

Confidence: High

Files:
- `Core/Store/MultiplayerGameStore.swift`
- `Features/Multiplayer/MultiplayerNightView.swift`

Evidence:
- `recordNightActions()` correctly computes `targetWasSaved` as “did any doctor protect the mafia target?”
- The same method then stores only one `doctorProtectedId`, preferring the majority/tiebreak target.
- `MultiplayerNightView.determineIfTargetWasSaved()` later recomputes save status from `mafiaTargetId == doctorProtectedId`.

Impact:
- If one doctor protects the mafia target and another protects someone else, `targetWasSaved` can be `true` during recording but `false` during resolution.
- The victim dies even though at least one doctor protected them.

Key references:
- `Core/Store/MultiplayerGameStore.swift:1988-1995`
- `Features/Multiplayer/MultiplayerNightView.swift:240-247`
- `Features/Multiplayer/MultiplayerNightView.swift:671-679`

Why players would feel this:
- “some people die when they shouldn’t” exactly matches this failure mode.

### MM-03 [High] Multiplayer role assignment events can be dropped during lobby with no recovery path for clients

Status: Fixed in app code path on 2026-03-21; build-verified

Verification update:
- `handlePlayerUpdate()` still ignores role-bearing `session_players` events while the local phase is `lobby`, preserving the stale-event guard for rematch/reset safety.
- `handleSessionUpdate()` now detects lobby-boundary transitions and forces a `refreshPlayers()` snapshot recovery when clients move from `lobby` into gameplay.
- The same boundary handling now refreshes players when returning from gameplay to `lobby`, clearing stale role, number, and mafia-teammate state if reset events were missed.
- App build verification succeeded after the fix.
- I did not find runnable multiplayer regression tests for this path in the current repo, and CLI `xcodebuild ... test` remains blocked because scheme `mafia_manager` is not configured for the test action.

Confidence: High

Files:
- `Core/Store/MultiplayerGameStore.swift`

Evidence:
- `handlePlayerUpdate()` discards any player update that contains a role while `currentPhase == "lobby"`.
- `handleSessionUpdate()` does not force a `refreshPlayers()` for non-host clients when phase changes out of lobby.

Impact:
- If role assignment updates arrive before the session phase update, non-host clients can miss their own role permanently for that round.
- Mafia teammates can remain empty.
- Players can land in role reveal or night with `myRole == nil`.

Key references:
- `Core/Store/MultiplayerGameStore.swift:600-635`
- `Core/Store/MultiplayerGameStore.swift:654-663`

Why players would feel this:
- some players cannot act at night;
- mafia players may not see teammates;
- phase flow appears stuck or inconsistent across devices.

### MM-04 [High] Multiplayer inspector rules are inconsistent and leak full roles

Status: Fixed in app and schema on 2026-03-21; build-verified. Live DB deployment pending.

Verification update:
- `MultiplayerNightView` now asks “Find out whether they are Mafia” and normalizes inspector results to `mafia`, `not_mafia`, or `blocked`.
- Known inspectors are excluded from the target list when the client has that information locally.
- The checked-in `submit_game_action` function now returns `blocked` for inspector-on-inspector checks and never returns full role strings for valid results.
- Live Supabase inspection still shows the deployed round-aware `submit_game_action(session_id, round_id, ...)` returning actual role names, so deployment is still required for the backend half of this fix.

Confidence: High

Files:
- `Features/Multiplayer/MultiplayerNightView.swift`
- `supabase/multiplayer_schema.sql`

Evidence:
- The inspector target list does not actually filter inspectors; the closure returns `true`.
- The RPC returns the target’s actual role string, including `doctor`, `citizen`, and `inspector`.
- The UI renders the full role string back to the player.

Impact:
- Multiplayer inspectors get more information than solo inspectors.
- Inspectors can identify other inspectors, which contradicts the solo logic and the project rules.

Key references:
- `Features/Multiplayer/MultiplayerNightView.swift:277-283`
- `Features/Multiplayer/MultiplayerNightView.swift:394-453`
- `supabase/multiplayer_schema.sql:467-476`

Why players would feel this:
- rule behavior differs between solo and multiplayer;
- two-inspector games leak information they should not.

### MM-05 [High] The documented multiplayer DB setup is incomplete, and missing RPCs break real gameplay paths

Status: Fixed in repo on 2026-03-21; build-verified for client fallbacks. Live DB deployment partially pending.

Verification update:
- `add_session_player` and `reset_players_ready` are now defined in `supabase/multiplayer_schema.sql` and in a dedicated migration so fresh setups no longer depend on unpublished dashboard state.
- `SessionService.addPlayer()` now falls back to direct table insertion if the RPC is missing.
- `SessionService.resetAllPlayersReady()` now falls back to a direct batch update instead of silently continuing in a broken state.
- `docs/MULTIPLAYER_GUIDE.md` now documents the required RPC contract instead of implying the schema only creates tables.
- Live Supabase inspection shows `reset_players_ready` is already present and `add_session_player` exists with an older body; applying the new migration is still required to align deployment with the checked-in contract.

Confidence: High

Files:
- `docs/MULTIPLAYER_GUIDE.md`
- `Core/Services/Multiplayer/SessionService.swift`
- `Core/Store/MultiplayerGameStore.swift`
- `supabase/migrations/20251202125630_add_reset_players_ready_rpc.sql`

Evidence:
- The guide tells users to run only `supabase/multiplayer_schema.sql`.
- `SessionService` requires `add_session_player` and `reset_players_ready`.
- `reset_players_ready` exists only in a later migration file.
- A repo-wide search found no definition for `add_session_player`, while the live Supabase project does have that function installed.
- `resetAllPlayersReady()` just logs and continues if the RPC is missing.

Impact:
- Fresh multiplayer setups from the repo docs can fail to join/create players because `add_session_player` is not versioned in the repo.
- Fresh setups can also fail to reset readiness between phases, which lets stale `isReady` values bleed into role reveal/night flow.
- Because the failure is swallowed, the app can continue in a broken state instead of surfacing a hard setup error.

Key references:
- `docs/MULTIPLAYER_GUIDE.md:24-31`
- `Core/Services/Multiplayer/SessionService.swift:520-532`
- `Core/Services/Multiplayer/SessionService.swift:561-567`
- `Core/Store/MultiplayerGameStore.swift:1742-1764`
- `supabase/migrations/20251202125630_add_reset_players_ready_rpc.sql:1-15`

Why players would feel this:
- role reveal can auto-advance or stay out of sync;
- lobby/session creation can fail depending on how the backend was provisioned.

### MM-06 [High] Solo night win condition disagrees with the documented/tests behavior

Status: Fixed in app on 2026-03-21; test-verified.

Verification update:
- `evaluateWinners(startOfDay:)` now ends the game when `mafiaCount >= nonMafiaCount` after night resolution.
- `xcodebuild test` now passes `testMafiaWins_WhenEqualOrMoreThanNonMafia`.

Confidence: Confirmed by snippet execution

Files:
- `Core/Store/GameStore.swift`
- `mafia_managerTests/GameStoreTests.swift`
- `docs/MULTIPLAYER_GUIDE.md`

Evidence:
- `evaluateWinners(startOfDay: true)` requires `mafiaCount > nonMafiaCount`.
- The project’s own test expects a 2-vs-2 state after night to end the game in Mafia’s favor.
- Multiplayer docs also define the win condition as `Mafia ≥ Non-mafia alive`.
- Snippet result:
  - after two kills in a 6-player game, `aliveMafia=2`, `aliveNonMafia=2`, `gameOver=false`.

Impact:
- Solo games can continue into an extra day even after the Mafia already has a decisive tie state.
- This is exactly the kind of “flow doesn’t move correctly” complaint players report.

Key references:
- `Core/Store/GameStore.swift:724-745`
- `mafia_managerTests/GameStoreTests.swift:198-208`

### MM-07 [High] Solo tied votes do not advance the day cycle

Status: Fixed in app on 2026-03-21; test-verified.

Verification update:
- `applyVotingResult()` now records a no-elimination `DayAction`, increments `dayIndex`, re-runs win evaluation, and only then transitions back to night.
- `xcodebuild test` now passes `testApplyVotingResult_TieStillAdvancesDay`.

Confidence: Confirmed by snippet execution

Files:
- `Core/Store/GameStore.swift`

Evidence:
- In `applyVotingResult()`, the tie/no-vote branch clears the voting session and jumps straight to `wakeUpRole(.mafia)`.
- It does not append a `DayAction`.
- It does not increment `dayIndex`.
- Snippet result after a 2-2 tie:
  - `dayIndex=0`
  - `dayHistory=0`
  - phase jumps straight to night.

Impact:
- day numbering repeats;
- analytics/history undercount days;
- any later logic depending on completed day count becomes unreliable.

Key references:
- `Core/Store/GameStore.swift:431-438`

Why players would feel this:
- the game appears to “skip” the day accounting after a tied vote.

### MM-08 [Medium] Solo inspector UI leaks blocked results even though store/test logic says it should not

Status: Fixed in app on 2026-03-21; test-verified.

Verification update:
- `GameStore.endNight()` no longer stores `.inspector` as a visible result for blocked inspector checks.
- `NightWakeUpView` now reads the recorded result from the store and shows a generic “Investigation Blocked” state when the result is intentionally withheld.
- `xcodebuild test` now passes `testInspectorCheck_CannotIdentifyOtherInspectors`.

Confidence: High

Files:
- `Features/Night/NightWakeUpView.swift`
- `Core/Store/GameStore.swift`
- `mafia_managerTests/GameStoreTests.swift`

Evidence:
- The view sets `investigationResult` directly from `target.role` before it asks the store to record the action.
- The store tries to block inspector-on-inspector identification.
- The test expects `inspectorResultRole` to stay nil for that case.

Impact:
- In games with multiple inspectors, the UI reveals “Police” immediately anyway.
- The store and the user-visible behavior disagree.

Key references:
- `Features/Night/NightWakeUpView.swift:716-735`
- `Core/Store/GameStore.swift:539-549`
- `mafia_managerTests/GameStoreTests.swift:198-208`

### MM-09 [Medium] Multiplayer voting readiness uses `isReady` flags instead of actual submitted votes

Status: Fixed in app code path on 2026-03-21; build-verified.

Verification update:
- `checkVotingPhaseReadiness()` now derives human readiness from the actual submitted vote actors instead of `isReady` flags.
- The voting UI no longer hides the “Waiting for other players...” state behind a successful ready-flag write.

Confidence: High

Files:
- `Features/Multiplayer/MultiplayerVotingView.swift`
- `Core/Store/MultiplayerGameStore.swift`

Evidence:
- Non-host humans are considered “done” only if `isReady == true`.
- The ready flag is set in a second request after the vote is submitted.
- The host readiness check does not fall back to the actual vote table for non-host humans the way night readiness falls back to action presence.

Impact:
- A vote can be stored successfully, but if the follow-up `setReadyStatus(true)` fails, the host can never end voting.
- From a player’s perspective, this looks like a vote that “didn’t count” or a voting phase that never unlocks.

Key references:
- `Features/Multiplayer/MultiplayerVotingView.swift:248-258`
- `Core/Store/MultiplayerGameStore.swift:2299-2316`

### MM-10 [Low] Morning summary labels the first night as “Night 2”

Status: Fixed in app on 2026-03-21; code-path verified.

Verification update:
- `MorningSummaryView` now renders `night.nightIndex` directly instead of `night.nightIndex + 1`.

Confidence: High

Files:
- `Features/Morning/MorningSummaryView.swift`

Evidence:
- `NightAction.nightIndex` starts at `1`.
- The summary header renders `night.nightIndex + 1`.

Impact:
- Player-facing logs and morning summaries are off by one from the first night onward.

Key references:
- `Features/Morning/MorningSummaryView.swift:15-23`

## Process / Coverage Failures

### MM-11 [High] The project’s automated test path is effectively disabled

Status: Fixed on 2026-03-21; `xcodebuild test` verified.

Verification update:
- Added a real `mafia_managerTests` unit-test target to the Xcode project.
- Updated the shared `mafia_manager` scheme so the test action includes that target.
- `xcodebuild -project mafia_manager.xcodeproj -scheme mafia_manager -destination "platform=iOS Simulator,name=iPhone 17 Pro" test` now succeeds with 17 passing tests.

Confidence: Confirmed by project audit and command output

Files:
- `mafia_manager.xcodeproj/xcshareddata/xcschemes/mafia_manager.xcscheme`
- `mafia_manager.xcodeproj/project.pbxproj`
- `mafia_managerTests/GameStoreTests.swift`

Evidence:
- The shared scheme has an empty `<Testables>` section.
- The `.pbxproj` contains only one native target: the app target.
- Running `xcodebuild -project mafia_manager.xcodeproj -scheme mafia_manager ... test` returns:
  - `Scheme mafia_manager is not currently configured for the test action.`

Impact:
- Regressions in core game logic are not being executed by the default Xcode test flow.
- The existing `GameStoreTests.swift` file is not protecting the app.

Key references:
- `mafia_manager.xcodeproj/xcshareddata/xcschemes/mafia_manager.xcscheme:25-31`
- `mafia_manager.xcodeproj/project.pbxproj:474-500`

## Lower-Priority Setup Drift

### MM-12 [Low] The optional performance migration is internally inconsistent

Status: Fixed in repo on 2026-03-21; build-verified. Live DB deployment pending.

Verification update:
- `SessionService.assignRolesAndNumbers()` now passes actual JSON to `batch_assign_roles` instead of a JSON string.
- `supabase/performance_migrations.sql` and the new migration file now remove the nonexistent `session_players.updated_at` write and pin `search_path`.
- Live Supabase inspection still shows the deployed `batch_assign_roles` writing `updated_at = NOW()`, so deployment is still required for the backend fast path.

Confidence: High

Files:
- `Core/Services/Multiplayer/SessionService.swift`
- `supabase/performance_migrations.sql`

Evidence:
- `batch_assign_roles` expects `JSONB`, but the client serializes the assignments to a JSON string before calling the RPC.
- The SQL function also writes `updated_at` on `session_players`, but that column does not exist in the base schema.
- The only reason players do not see this immediately is that the client falls back to sequential updates when the RPC fails.
- Live Supabase inspection confirms the deployed database still has this function while `session_players` still has no `updated_at` column.

Impact:
- The “fast path” for role assignment is broken even if the migration is installed.
- Startup latency is higher than intended and failures are hidden in logs.

Key references:
- `Core/Services/Multiplayer/SessionService.swift:639-677`
- `supabase/performance_migrations.sql:15-56`

## Second-Pass Findings

### MM-13 [High] Multiplayer game over never relaxes role privacy, so non-host players cannot see final roles

Status: Fixed in app and schema on 2026-03-21; build-verified. Live DB deployment pending.

Verification update:
- `get_visible_role()` in the checked-in schema now reveals final roles once `current_phase = 'game_over'` or `is_game_over = true`.
- `MultiplayerGameStore` now forces a player snapshot refresh on the transition into `game_over`, so clients actually pull the newly visible roles.
- Live Supabase inspection still shows the deployed `get_visible_role()` without the `game_over` visibility rule.

Confidence: High

Files:
- `Features/Multiplayer/MultiplayerGameOverView.swift`
- `supabase/multiplayer_schema.sql`

Evidence:
- `MultiplayerGameOverView` expects `allPlayers` to contain each player’s role and renders a role chip only when `player.role` is non-nil.
- `SessionService.getSessionPlayers()` always reads through `game_session_players`.
- `get_visible_role()` never grants “game over” visibility; it only reveals roles to the player themself, fellow mafia, or the host.

Impact:
- Non-host players do not get a true final standings screen.
- Many role badges remain blank even after the game ends.
- Endgame information differs between host and clients.

Key references:
- `Features/Multiplayer/MultiplayerGameOverView.swift:22-34`
- `Features/Multiplayer/MultiplayerGameOverView.swift:68-71`
- `supabase/multiplayer_schema.sql:326-369`
- `Core/Services/Multiplayer/SessionService.swift:453-467`

### MM-14 [Medium] Multiplayer game-over winner can be temporarily or permanently blank after a night win

Status: Fixed in app and schema on 2026-03-21; build-verified. Live DB deployment pending.

Verification update:
- `resolve_night_atomic()` in the checked-in schema now accepts `p_is_game_over` / `p_winner` and writes winner state in the same transaction as the phase transition.
- `SessionService.resolveNightAtomic()` now passes those fields to the RPC instead of issuing a second update.
- `MultiplayerGameOverView` now falls back to `currentPhaseData.gameOver(winner:)` if `currentSession.winner` is temporarily nil.
- Live Supabase inspection still shows the deployed `resolve_night_atomic()` with the old 5-argument signature and the old split-write behavior.

Confidence: High

Files:
- `Features/Multiplayer/MultiplayerGameOverView.swift`
- `Core/Services/Multiplayer/SessionService.swift`

Evidence:
- `MultiplayerGameOverView` reads the winner only from `currentSession?.winner`.
- `resolveNightAtomic()` updates phase/phaseData first and then does a second write for `is_game_over` and `winner`.
- The phase data already contains `.gameOver(winner: ...)`, but the view ignores it.

Impact:
- After a night win, clients can reach the game-over screen and still render `winner == nil`.
- If the follow-up winner update is delayed or missed, the UI shows “GAME ENDED / No Winner Determined” even when the phase data already knows the winner.

Key references:
- `Features/Multiplayer/MultiplayerGameOverView.swift:13-15`
- `Core/Services/Multiplayer/SessionService.swift:420-441`

### MM-15 [Medium] Multiplayer night death reveal can mislabel dead special roles as Citizen

Status: Fixed in app code path on 2026-03-21; build-verified.

Verification update:
- `NightActionRecord` now carries `revealed_death_roles`, populated from authoritative host-side role data when the night resolves.
- `MultiplayerDeathRevealView` now prefers that explicit revealed-role map before falling back to role-participant inference.

Confidence: High

Files:
- `Features/Multiplayer/MultiplayerLobbyView.swift`

Evidence:
- `MultiplayerDeathRevealView.roleFor(player:)` falls back to inferring role from `mafiaPlayerNumbers`, `doctorPlayerNumbers`, and `inspectorPlayerNumbers`.
- Those arrays only contain players who actually submitted actions that night.
- A killed doctor or inspector who failed to submit will not appear in those arrays and is therefore classified as `.citizen`.

Impact:
- Death reveal can show the wrong role to non-host players.
- This is most likely in AFK / reconnect / missed-action scenarios, which are already the unstable edge cases players complain about.

Key references:
- `Features/Multiplayer/MultiplayerLobbyView.swift:659-680`
- `Features/Multiplayer/MultiplayerLobbyView.swift:727-741`

### MM-16 [Low] Documentation still claims `phase_timers` exists, but the live project has no such table

Status: Fixed in docs on 2026-03-21.

Verification update:
- `docs/MULTIPLAYER_GUIDE.md` no longer claims the schema creates `phase_timers` or instructs operators to enable Realtime on a nonexistent table.
- `docs/RLS_FIX_SUMMARY.md` now marks `phase_timers` references as legacy-only.

Confidence: High

Files:
- `docs/MULTIPLAYER_GUIDE.md`
- `supabase/multiplayer_schema.sql`

Evidence:
- The guide says the multiplayer schema creates `phase_timers` and tells operators to enable Realtime for it.
- The live Supabase project currently has no `public.phase_timers` table.
- The checked-in `multiplayer_schema.sql` also has no `phase_timers` definition.

Impact:
- Backend setup instructions are misleading.
- Anyone debugging timer-related behavior is pointed at a table that does not exist.

Key references:
- `docs/MULTIPLAYER_GUIDE.md:33-52`

## Remaining Deployment Order

1. Apply `supabase/migrations/20260321153000_fix_multiplayer_rpc_contract.sql` to the live project.
2. Apply `supabase/migrations/20260321154500_fix_performance_rpcs.sql` to the live project.
3. Re-run live Supabase inspection for `get_visible_role`, round-aware `submit_game_action`, `resolve_night_atomic`, and `batch_assign_roles`.
4. Smoke-test one real multiplayer room to confirm final-role reveal, inspector privacy, and night-win game-over winner rendering on deployed infrastructure.
